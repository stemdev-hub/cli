import type { AddRefResult } from '@stem/types';

// TODO: Insert block transclusion references into view files.
export async function addBlockToView(blockId: string, viewId: string): Promise<AddRefResult> {
  void blockId;
  void viewId;
  throw new Error('TODO: implement add reference operation.');
}
