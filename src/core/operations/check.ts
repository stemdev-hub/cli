import type { CheckResult } from '@stem/types';

// TODO: Run read-only validation against parsed files and the dynamic graph.
export async function checkProject(): Promise<CheckResult> {
  throw new Error('TODO: implement check operation.');
}
