import type { StemGraph } from '@stem/types';

// TODO: Traverse graph relationships with visited-set cycle protection.
export function traverseGraph(graph: StemGraph, startId: string): string[] {
  void graph;
  void startId;
  throw new Error('TODO: implement graph traversal.');
}
