{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Stem VS Code Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}/extensions/vscode-stem",
        "${workspaceFolder}"
      ]
    }
  ]
}
